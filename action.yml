name: "pull-request-auto-reviewer"
description: "auto post comments based on changes"
author: pshergie
branding:
  icon: "message-square"
  color: "yellow"
inputs:
  token:
    description: "token for github"
    required: true
  datapath:
    description: "path to data yaml file"
    required: true
  signature:
    description: a string that prepends to the message
    default: "🗯️ [pull-request-auto-reviewer]:"
runs:
  using: "node20"
  main: "dist/index.js"
